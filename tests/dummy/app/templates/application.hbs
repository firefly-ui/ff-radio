<h2 id="title">Welcome to Ember</h2>

{{ff-radio value=1 groupValue=someValue disabled=disabled on-change=(action (mut someValue))}}
{{ff-radio value=2 groupValue=someValue disabled=disabled on-change=(action (mut someValue))}}
{{ff-radio value=3 groupValue=someValue disabled=disabled on-change=(action (mut someValue))}}

<div>
  <button {{action 'setValue' 3}}>Set Value</button>
  Group Value: {{someValue}}
</div>

<div>
  <button {{action "toggleProperty" "disabled"}}>Toggle Disabled</button>
  Disabled: {{disabled}}
</div>
